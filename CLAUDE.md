# Test AI prototypes

This repository is a kit for building clickable Test AI prototypes that validate a solution before development.

Whenever the user asks to build or update a prototype, follow `MASTER.md` and use the `build-prototype` skill. Prototypes are throwaway checks of a solution, never shipped to production.
